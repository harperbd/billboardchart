<%= @song.id ? "Edit Song Listing" : "Add New Song" %>

<%= form_for [@artist, @song] do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :artist_id %>
  <%= f.text_field :artist_id %>
  <%# <%= f.select 'artist_id', options_for_select(@artists.collect{ |artist| [artist.name] }) %>

  <%= f.submit %>
  <% end %>